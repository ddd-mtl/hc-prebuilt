{
  "name": "hc-prebuilt-dev",
  "private": true,
  "scripts": {
    "version"          : "./hc --version",
    "install:rust"      : "bash install-rust.sh",
    "install:hc-cli"    : "bash install-hc-cli.sh 0.1.0 bin",
    "install:hash-zome" : "bash install-hash-zome.sh bin",
    "copy-binaries"     : "bash copy-binaries.sh electron/bin"
  },
  "dependencies": {
    "@lightningrodlabs/electron-holochain": "^0.6.1"
  }
}
